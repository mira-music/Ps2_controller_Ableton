"""PySide6 hardware-style UI prototype for FX Machine.

This package deliberately lives alongside ``src.ui`` while the existing Tkinter
interface remains the production UI.  It contains no OSC or controller logic;
that integration starts only after the visual system has been approved.
"""
